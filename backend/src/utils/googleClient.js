// backend/src/utils/googleClient.js (New File)

import { google } from "googleapis";

// Re-export the necessary module constants
export const calendar = google.calendar;
export const auth = google.auth;
